export enum SCREEN_PATHS {
  LOGIN = "/login",
  SIGNUP = "/signup",

  DASHBOARD = "/dashboard",

  CLIENTS = "/clients",
  DOCUMENT_REQUESTS = "/document-requests",

  TEMPLATES = "/templates",

  SETTINGS = "/settings",
}
