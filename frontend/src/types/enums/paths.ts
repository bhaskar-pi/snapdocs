export enum SCREEN_PATHS {
  LOGIN = "/login",
  SIGNUP = "/signup",

  DASHBOARD = "/dashboard",

  CLIENTS = "/clients",
  DOCUMENTS_REQUEST = "/documents-request",

  TEMPLATES = "/templates",

  SETTINGS = "/settings",
}
